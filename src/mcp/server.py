from src.mcp.tools import mcp

if __name__ == "__main__":
    # Startet den MCP Server im lokalen Stdio-Modus (Standard für lokale Integration)
    print("Starting E-Commerce Research MCP Server...")
    mcp.run()
