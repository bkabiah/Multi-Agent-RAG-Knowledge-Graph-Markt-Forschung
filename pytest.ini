[pytest]
# Fügt das aktuelle Verzeichnis zum Python-Suchpfad hinzu, damit 'src' gefunden wird
pythonpath = .
# Definiert, wo die Tests liegen
testpaths = tests
# Zeigt detaillierte Informationen bei Fehlern
addopts = -v --tb=short
